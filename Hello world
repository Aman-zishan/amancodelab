#include <stdio.h>
int main() 
{
   // printf() displays the string inside quotation
  int i,n;
  n=0;
  printf("Enter the number of hello world to be printed");
  scanf("%d",&n);
  for(i=0;i<n;i++)
  printf("Hello, World!");
   return 0;
}
